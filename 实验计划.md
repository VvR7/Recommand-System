# 推荐系统实验计划 — KuaiRec 数据集

> 最后更新：2026-06-02

## 1. 实验定位

| 角色 | 方法 | 说明 |
|------|------|------|
| **🎯 主方法** | **SASRec 系列** (SASRec → SASRecF → SASRecP) | 单向注意力 + 特征增强 + 三项创新模块 |
| **📊 时序对比** | BERT4Rec 系列 (BERT4Rec, BERT4RecF) | 双向注意力 Cloze masking 作为对照 |
| **📊 非时序基线** | DeepFM, NeuMF, BPR, ItemKNN | 传统方法对比 |

**为什么选 SASRec 作为主方法基座**：
- SASRec (0.8249) 略优于 BERT4Rec (0.8217)，单向注意力在此数据集无劣势
- 训练时间约为 BERT4Rec 的 60%（无 Cloze masking 开销）
- Next-item prediction 更适合特征注入（无 MASK token 冲突）
- 时间间隔 bias 在因果注意力中语义更自然

---

## 2. 数据集

### 2.1 数据概览

| 维度 | 数值 | 说明 |
|------|------|------|
| 名称 | KuaiRec 2.0 | 快手短视频推荐数据集 |
| 训练集来源 | `big_matrix.csv` | 5,765 用户 × 4.17M 交互 |
| 测试集来源 | `small_matrix.csv` | 1,129 用户（与训练完全不重叠） |
| 全局物品数 | 3,310 | 两个矩阵共享同一物品空间 |
| 交互密度 | ~21.9% | 相对稠密 |
| 人均交互 | ~723 条 | 序列较长，适合时序建模 |

### 2.2 数据规模定位

**4.17M 交互 — 百万级**，约为 MovieLens-1M 的 4 倍。对传统方法充足，对序列 Transformer 方法中等偏大（瓶颈在 CPU RAM 而非 GPU）。

### 2.3 附加特征

| 特征类型 | 来源 | 字段数 | 评测可用？ |
|---------|------|--------|-----------|
| 用户特征 | `user_features.csv` | 12 token 字段 | ❌ 跨用户不可用（仅 DeepFM 训练辅助） |
| 物品类别特征 | `kuairec_caption_category.csv` | 3 层级（39/109/156 类） | ✅ 训练+评测可用 |
| 社交网络 | `social_network.csv` | 472 用户（覆盖率 ~6.6%） | ❌ 覆盖率太低 |
| 物品逐日特征 | `item_daily_features.csv` | 58 列 × 63 天 | ⚠️ 需时序对齐（未使用） |

### 2.4 数据预处理

1. **过滤**：k-core ≥5（user/item），watch_ratio ∈ [0.7, 2.0]
2. **物品空间统一**：以小矩阵的 3,310 个物品为准，大矩阵只保留这些物品的交互
3. **原子文件生成**：
   - `kuairec.inter`（交互）：`preprocess.py`
   - `kuairec.user`（用户特征）：`preprocess_features.py`
   - `kuairec.item`（物品类别特征）：`preprocess_features.py`

---

## 3. 评测协议

### 3.1 时序留一法（核心协议）

```
对每个测试用户：
  1. 按 timestamp 排序所有正例物品
  2. 前 80% → 历史（构建用户画像/输入序列）
  3. 后 20% → 评测目标（不在历史中，零泄漏）
  4. 困难负例：该用户 wr < 0.3 的物品，固定采样 99 个（seed=42）
  5. 用户内取所有评测目标的均值，跨用户取整体均值
```

### 3.2 评测指标

**主指标**：GAUC（用户级 AUC 均值，消除用户间正例数差异）

**完整指标集**（所有方法均报告 7 个指标）：

| 指标 | 含义 | K 取值 |
|------|------|--------|
| **GAUC** | 每用户 AUC 均值 | — |
| **Recall@K** | 候选池内正例命中率 | 5, 10, 20 |
| **NDCG@K** | 带位置折扣的排序质量 | 5, 10, 20 |

### 3.3 评测脚本

`evaluate_temporal.py` — 统一评测入口，支持所有模型，各自有专用的向量化 Scorer。

---

## 4. 方法矩阵

### 4.1 🎯 主方法：SASRec 系列

| 方法 | GAUC | Recall@10 | 说明 | 状态 |
|------|------|-----------|------|------|
| **SASRec** | 0.8249 | 0.5610 | 单向 Transformer，ID-only 基线 | ✅ |
| **SASRecF** | **0.8509** | **0.5955** | + item cat features | ✅ |
| **SASRecP** 全开 | — | — | SASRecF + RoPE + TimeBias + Contrastive | 🔧 训练中 |
| SASRecP 消融 ×7 | — | — | 每个模块独立评估 | ⏳ 待跑 |

**SASRec → SASRecF 提升：+3.2% GAUC，+6.1% Recall@10**

### 4.2 📊 对比：BERT4Rec 系列

| 方法 | GAUC | Recall@10 | 说明 | 状态 |
|------|------|-----------|------|------|
| **BERT4Rec** | 0.8217 | 0.5274 | 双向 Transformer, ID-only | ✅ |
| **BERT4RecF** | 0.7963 | 0.4790 | + item cat features | ✅（确认掉点） |

> BERT4RecF 掉点（-3.1%）已确认为**结构性设计限制**而非 bug：Cloze masking 训练时 20% 位置无法注入真实特征，导致训练-推理分布不一致。这是论文中有价值的负样本发现——**特征注入的适用性取决于注意力范式**。BERT4Rec 系列仅作为双向注意力对照组。

### 4.3 📊 基线：传统方法

| 方法 | GAUC | 类型 | 状态 |
|------|------|------|------|
| DeepFM+Feat | 0.7067 | FM+MLP + user/item cat | ✅ |
| DeepFM | 0.6758 | FM+MLP ID-only | ✅ |
| NeuMF | 0.6480 | GMF+MLP | ✅ |
| BPR | 0.6389 | 矩阵分解 | ✅ |
| ItemKNN | 0.5525 | 协同过滤 | ✅ |

---

## 5. SASRecP 三项创新模块

### 5.1 模块概览

| # | 模块 | 配置开关 | 原理 | 新参数 |
|---|------|---------|------|--------|
| 1 | **RoPE** | `use_rope` | 旋转位置编码，attention 依赖相对距离 (j-i) | — |
| 2 | **Time Interval Bias** | `use_time_bias` | 真实时间戳计算 pairwise 时间差，加入 attention bias | `time_bias_method` (log/linear) |
| 3 | **Contrastive Loss** | `use_contra_loss` | InfoNCE 辅助任务，同 category 物品 embedding 更近 | `contra_lambda`, `contra_temperature`, `contra_weighting` (binary/hierarchical) |

### 5.2 消融实验矩阵（8 组）

| # | 实验 | use_rope | use_time_bias | use_contra_loss | 目的 |
|---|------|----------|---------------|-----------------|------|
| 1 | **全开** | ✅ | ✅ | ✅ | 完整模型 |
| 2 | 基线 (=SASRecF) | ❌ | ❌ | ❌ | 验证无退化 |
| 3 | 仅 RoPE | ✅ | ❌ | ❌ | 位置编码贡献 |
| 4 | 仅 TimeBias | ❌ | ✅ | ❌ | 时间间隔贡献 |
| 5 | 仅 Contra | ❌ | ❌ | ✅ | 对比学习贡献 |
| 6 | RoPE+Time | ✅ | ✅ | ❌ | 位置+时间交互 |
| 7 | RoPE+Contra | ✅ | ❌ | ✅ | 位置+语义交互 |
| 8 | Time+Contra | ❌ | ✅ | ✅ | 时间+语义交互 |

### 5.3 RoPE 分析维度

长短序列用户对比（验证 RoPE 的核心假设）：

| 用户分组 | 序列长度 | 绝对位置编码 | RoPE 预期 |
|---------|---------|------------|-----------|
| 短序列（<50 items） | padding 主导 | 位置语义错配 | ✅ 显著改善 |
| 中序列（50-150） | 混合 | 基线 | ✅ 小幅改善 |
| 长序列（>200，截断） | 远端截断 | 远端信号丢失 | ✅ 改善（相对距离保留） |

### 5.4 Contrastive Loss 对比维度

| 对比 | binary | hierarchical |
|------|--------|-------------|
| 同 L3 | weight=1 | weight=1.0 |
| 同 L2 only | weight=1 | weight=0.5 |
| 同 L1 only | weight=1 | weight=0.2 |
| 不同 L1 | weight=0 | weight=0 |

---

## 6. 模型配置详情

### 6.1 SASRec 系列

| 参数 | SASRec | SASRecF | SASRecP |
|------|--------|---------|---------|
| hidden_size | 256 | 256 | 256 |
| n_layers / n_heads | 2 / 4 | 2 / 4 | 2 / 4 |
| inner_size | 1024 | 1024 | 1024 |
| hidden_dropout / attn_dropout | 0.5 / 0.5 | 0.5 / 0.5 | 0.5 / 0.5 |
| loss_type | BPR | BPR | BPR |
| selected_features | — | L1+L2+L3 | L1+L2+L3 |
| batch_size | 256 | 128 | 128 |
| epochs | 100 | 100 | 100 |
| use_rope | — | — | True |
| use_time_bias | — | — | True |
| use_contra_loss | — | — | True |

### 6.2 BERT4Rec 系列（对比）

| 参数 | BERT4Rec | BERT4RecF |
|------|----------|-----------|
| hidden_size | 256 | 256 |
| n_layers / n_heads | 2 / 4 | 2 / 4 |
| mask_ratio | 0.2 | 0.2 |
| loss_type | CE | CE |
| selected_features | — | L1+L2+L3 |
| batch_size | 4096 | 2048 |
| epochs | 100 | 100 |

### 6.3 传统方法（基线）

| 参数 | DeepFM | NeuMF | BPR | ItemKNN |
|------|--------|-------|-----|---------|
| embedding_size | 256 | 256 | 256 | — |
| mlp_hidden_size | [256,128,64] | [256,128,64] | — | — |
| epochs | 50 | 100 | 100 | — |
| batch_size | 4096 | 4096 | 4096 | — |

---

## 7. 代码组织

```
RecommandSystem/
├── configs/
│   ├── sasrec_kuairec.yaml / sasrecf_feat_kuairec.yaml / sasrecp_kuairec.yaml
│   ├── bert4rec_kuairec.yaml / bert4recf_feat_kuairec.yaml
│   ├── bpr_kuairec.yaml / neumf_kuairec.yaml / itemknn_kuairec.yaml
│   └── deepfm_kuairec.yaml / deepfm_feat_kuairec.yaml
├── train_cf.py                      # 通用训练（--model + --config）
├── evaluate_temporal.py             # 时序留一法统一评测
├── preprocess_features.py           # .user / .item 原子文件生成
├── dataset/kuairec/
│   ├── kuairec.inter/.user/.item
│   └── *_data.pkl
└── id_only_baseline/                # 归档

RecBole 自定义模型（服务器 pip 路径）:
  recbole/model/sequential_recommender/sasrecp.py
  recbole/model/sequential_recommender/bert4recp.py (已实现，视需要)
  recbole/model/sequential_recommender/bert4recf.py (含 MASK fix)
  recbole/model/layers.py (新增 RoPE, time_bias, **kwargs)
```

---

## 8. 本地 vs 服务器

### 本机（8 GB CPU RAM + 8 GB GPU VRAM）

| 模型 | CPU RAM | GPU VRAM | 训练时间 |
|------|---------|----------|---------|
| ItemKNN | <1 GB | <1 GB | <1 min |
| BPR / NeuMF | ~2 GB | ~2 GB | ~20-30 min |
| DeepFM / +Feat | ~3 GB | ~2 GB | ~50 min |

### 需服务器（>16 GB CPU RAM）

| 模型 | CPU RAM | GPU VRAM | 训练时间 |
|------|---------|----------|---------|
| SASRec 系列 | >16 GB | ~5-8 GB | ~5-10 h |
| BERT4Rec 系列 | >16 GB | ~6-10 GB | ~8-15 h |

---

## 9. 实验路线图

### Phase 1：特征增强 ✅ 完成

| 任务 | 状态 |
|------|------|
| SASRecF 训练 + 评测 → **GAUC 0.8509 (+3.2%)** | ✅ |
| BERT4RecF 训练 + 评测 → GAUC 0.7963（MASK bug，待重训） | ⚠️ |
| → 结论：**特征增强在 SASRec 上效果显著，SASRecF 是新的最佳基座** | ✅ |

### Phase 2：SASRecP 创新验证 🔧 进行中

| 任务 | 说明 |
|------|------|
| SASRecP 全开训练 + 评测 | 验证三项创新综合效果 |
| 消融实验 ×7 | 每个模块独立贡献 |
| RoPE 长短序列用户分析 | 验证 "为什么 RoPE 有用" |
| Contrastive weighting 对比 | binary vs hierarchical |

### Phase 3：分析完善（可选）

- [ ] 用户分组分析（按活跃度、序列长度）
- [ ] 冷启动分析
- [ ] Beyond-Accuracy 指标（Coverage, Gini, Avg Popularity）

---

## 10. 服务器运行命令

### SASRecF（进行中）

```bash
CUDA_VISIBLE_DEVICES=X python train_cf.py --model SASRecF --config configs/sasrecf_feat_kuairec.yaml
python evaluate_temporal.py --checkpoint saved/SASRecF-*.pth --model SASRecF --split test
```

### BERT4RecF（对比，待重训）

```bash
CUDA_VISIBLE_DEVICES=X python train_cf.py --model BERT4RecF --config configs/bert4recf_feat_kuairec.yaml
python evaluate_temporal.py --checkpoint saved/BERT4RecF-*.pth --model BERT4RecF --split test
```

### SASRecP 系列（主实验）

```bash
# 全开
CUDA_VISIBLE_DEVICES=X python train_cf.py --model SASRecP --config configs/sasrecp_kuairec.yaml
python evaluate_temporal.py --checkpoint saved/SASRecP-*.pth --model SASRecP --split test

# 消融（通过命令行覆盖配置开关）
CUDA_VISIBLE_DEVICES=X python train_cf.py --model SASRecP \
    --config configs/sasrecp_kuairec.yaml \
    --config_dict '{"use_rope":true,"use_time_bias":false,"use_contra_loss":false}'
```

### 服务器部署清单

本地需同步到服务器的文件：
```
evaluate_temporal.py
train_cf.py
configs/sasrecp_kuairec.yaml
configs/bert4recp_kuairec.yaml
recbole/model/layers.py                    → pip 路径
recbole/model/sequential_recommender/sasrecp.py   → pip 路径
recbole/model/sequential_recommender/bert4recp.py  → pip 路径
recbole/model/sequential_recommender/bert4recf.py  → pip 路径（含 MASK fix）
```

---

## 11. 注意事项

| 问题 | 处理 |
|------|------|
| NumPy 兼容性（`np.long` 已移除） | 已 patch: `np.long` → `np.int64` |
| Big matrix 与 small matrix 用户不重叠 | 跨用户评估协议，用户特征不可用于评测 |
| RecBole labeled 模式不兼容 | 已放弃，改用自建时序留一法 |
| BERT4RecF MASK token 特征泄漏 | 已修复：mask 位置 feature_emb 置零 |
| SASRecF scorer concat_layer 命名 | 已修复：兼容 `feat_concat_layer` 和 `concat_layer` |
| SASRecP 评估时无 timestamp？ | 已修复：`evaluate_temporal.py` 提取并传入 timestamp |
